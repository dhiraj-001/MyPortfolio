import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['assets.aceternity.com'], // Add the domain you need to allow
  },
  // Add other config options here
};

export default nextConfig;
